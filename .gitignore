# ---------- Android / Gradle ----------
*.iml
.gradle/
build/
captures/
.externalNativeBuild/
.cxx/
local.properties
.idea/
!.idea/codeStyles/

# Firma release: file con le password del keystore, MAI committare.
# L'esempio (keystore.properties.example) resta versionato.
keystore.properties
*.jks
*.keystore

# Archivi / collegamenti locali finiti nel progetto
*.zip
*.lnk

# ---------- Web (React) ----------
node_modules/
dist/
dist-ssr/
.vite/
coverage/
*.local
*.tsbuildinfo

# Variabili d'ambiente: si committano solo l'esempio e la configurazione di
# pubblicazione, che non contiene segreti. Restano fuori .env e .env.local.
.env
.env.*
!.env.example
!.env.production

# ---------- Firebase ----------
.firebase/
firebase-debug.log*
firestore-debug.log*
database-debug.log*

# ---------- Claude Code ----------
.claude/settings.local.json

# ---------- File di lavoro locali (NON pubblicare) ----------
# Contiene dettagli di vulnerabilita' non ancora fixate: repo pubblico, resta locale.
AUDIT_TODO.md

# ---------- Sistema / editor ----------
.DS_Store
Thumbs.db
desktop.ini
*.log
*.swp
.vscode/*
!.vscode/extensions.json
